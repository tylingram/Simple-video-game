services:
  - type: web
    name: island-rts-server
    env: python
    rootDir: server
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    plan: free
    healthCheckPath: /
